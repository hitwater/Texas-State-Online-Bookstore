import template from './cart.html';
import controller from './cart.controller';
import './cart.styl';

let cartComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default cartComponent;
